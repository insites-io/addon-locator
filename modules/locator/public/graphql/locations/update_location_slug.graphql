mutation update_location_slug($id: ID!, $slug: String!) {
  items: record_update(
    id: $id
    record: {
      table: "modules/insites_locator/location"
      properties: [
        { name: "slug", value: $slug }
      ]
    }
  ) {
    id
    properties
  }
}
